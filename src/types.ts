export interface SearchFilterProps {
    icon: string;
    title: string;
    subtitle: string;
  }
  
  export interface NavigationLinkProps {
    href: string;
    children: React.ReactNode;
  }